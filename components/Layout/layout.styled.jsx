import styled from "styled-components";

export const StyledSection = styled.section`
  max-width: 60rem;
  margin-left: auto;
  margin-right: auto;

  a:hover {
    cursor: pointer;
  }

  .homeLink {
    display: block;
    text-align: center;
  }
`;
