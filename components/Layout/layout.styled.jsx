import styled from "styled-components";

export const StyledSection = styled.section`
  a:hover {
    cursor: pointer;
  }

  .homeLink {
    display: block;
    text-align: center;
  }

  .blob {
    position: absolute;
    top: -3rem;
    right: -2rem;
    width: 10rem;
    height: 9rem;
  }
`;
